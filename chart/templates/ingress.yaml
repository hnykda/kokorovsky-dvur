apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kokorovskydvur
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - secretName: kokorovskydvur-tls
      hosts:
        - {{ .Values.host }}
        {{- range .Values.extraHosts }}
        - {{ . }}
        {{- end }}
  rules:
    - host: {{ .Values.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kokorovskydvur
                port:
                  number: 80
    {{- range .Values.extraHosts }}
    - host: {{ . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kokorovskydvur
                port:
                  number: 80
    {{- end }}
